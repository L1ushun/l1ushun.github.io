# 静态网页文件夹

这个文件夹用于存放非博客类的静态网页文件（HTML、CSS、JS 等）。

## 说明

- 此文件夹下的所有文件会被直接复制到 `public/static-pages/` 目录
- 不会被 Hexo 渲染处理，保持原始文件内容
- 可以通过 `https://l1ushun.github.io/static-pages/文件名.html` 访问

## 使用示例

将你的静态网页文件（如 `demo.html`、`project/index.html` 等）放在此文件夹下即可。

